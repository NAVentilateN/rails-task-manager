<h1><%= @task.title %></h1>
<p>
<%= @task.details %>
</p>
<p>
<%= "My task is #{@task.completed ? 'completed' : 'incomplete'}." %>
</p>

<%= link_to 'home', tasks_path %>
<%= link_to 'edit', edit_task_path %>
<%= link_to 'delete',
    task_path(@task),
    method: :delete,
    data: { confirm: "Are you sure?" }  %>
