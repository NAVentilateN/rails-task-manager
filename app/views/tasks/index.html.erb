<h1> My task </h1>
<% @tasks.each do |task| %>
  <li><input type= checkbox <%= task.completed ? 'checked' : '' %>> <%= link_to task.title, task_path(task) %></li>
<% end %>

<%= link_to 'new', new_task_path %>
